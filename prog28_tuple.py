# tuples (cannot change values)
# also called immutables
numbers = (1, 2, 3)
# we can only get the information about a tuple
# like dot count or dot index
# cannot append, insert etc

# note: tuple is used when we don't want our list to be updated
